name: Pulsar NBA CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Install
        run: python -m pip install .
      - name: Compile
        run: python -m py_compile nba/*.py
      - name: Preflight
        run: python -m nba.preflight
      - name: Deterministic end-to-end dry-run
        run: python -m nba.e2e_dryrun
      - name: Unit tests
        run: python -m unittest discover -s tests -v
